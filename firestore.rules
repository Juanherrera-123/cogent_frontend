rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /resultadosCogent/{document=**} {
      allow read, write: if true;
    }
    match /credencialesCogent/{document=**} {
      allow read, write: if true;
    }
  }
}
